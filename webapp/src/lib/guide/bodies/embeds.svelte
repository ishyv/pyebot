<script lang="ts">
import Callout from "$lib/guide/components/Callout.svelte";
</script>

<p>
  the embeds feature lets you build rich formatted messages from the dashboard
  and send, schedule, or sticky them in any channel. use it for announcements,
  rules, welcome messages, or any content you want to look polished.
</p>

<p>
  create an embed, pick a channel, and either send it immediately or set a time
  for it to post. stickied embeds are reposted automatically when new messages
  push them off the screen.
</p>

<h2>what admins do</h2>
<p>
  draft the message, preview it, save named embeds, send announcements, schedule
  future posts, and keep important messages sticky in busy channels.
</p>

<h2>good uses</h2>
<p>
  use embeds for rules, welcome panels, event posts, marketplace notices, role
  instructions, and any message that should look deliberate instead of pasted in
  a panic at midnight.
</p>

<Callout variant="tip">
  the embeds page shows a live preview of your embed as you edit it, so you can
  see exactly how it will look before sending.
</Callout>

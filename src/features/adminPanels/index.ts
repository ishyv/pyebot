import { defineFeature } from "@/framework";

export default defineFeature({
  id: "adminPanels",
  name: "Admin Panels",
  description: "Admin dashboard and server configuration commands.",
  defaultEnabled: true,
});

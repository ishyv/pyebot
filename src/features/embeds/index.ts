import { defineFeature } from "@/framework";

export default defineFeature({
  id: "embeds",
  name: "Embed Creator",
  description: "Create, schedule, and sticky custom embeds.",
  defaultEnabled: true,
});

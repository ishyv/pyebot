import { defineFeature } from "@/framework";

export default defineFeature({
  id: "scripts",
  name: "Scripts",
  description: "Author and run TypeScript scripts that query and act on the server.",
  defaultEnabled: true,
});
